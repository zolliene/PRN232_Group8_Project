@* 
@page
@model PatientProfileModel
@{
    ViewData["Title"] = "Thông tin cá nhân";
}

<div class="d-flex justify-content-center align-items-center mt-5 mb-5">
    <div class="card p-5 shadow-lg bg-light rounded" style="min-width: 800px;">
        <div class="text-center py-3 mb-4 rounded" style="background-color: #d0e7ff; color: #0d6efd;">
            <h2 class="m" style="color:white" >Thông tin cá nhân</h2>
        </div>

        @if (Model.SuccessMessage != null)
        {
            <div class="alert alert-success text-center">@Model.SuccessMessage</div>
        }

        <form method="post">
            <div class="row mb-3">
                <div class="col-md-6">
                    <label class="form-label">Họ</label>
                    <input asp-for="Profile.FirstName" class="form-control" placeholder="Nhập họ" />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Tên</label>
                    <input asp-for="Profile.LastName" class="form-control" placeholder="Nhập tên" />
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-6">
                    <label class="form-label">Ngày sinh</label>
                    <input asp-for="Profile.Dob" type="date" class="form-control" />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Giới tính</label>
                    <select asp-for="Profile.Gender" class="form-select">
                        <option>Male</option>
                        <option>Female</option>
                        <option>Other</option>
                    </select>
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-6">
                    <label class="form-label">Số điện thoại</label>
                    <input asp-for="Profile.Phone" class="form-control" placeholder="Ví dụ: 0987654321" />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Địa chỉ</label>
                    <input asp-for="Profile.Address" class="form-control" placeholder="Nhập địa chỉ" />
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-6">
                    <label class="form-label">Mã bảo hiểm</label>
                    <input asp-for="Profile.InsuranceNumber" class="form-control" placeholder="Nhập mã bảo hiểm" />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Email</label>
                    <input asp-for="Profile.Email" class="form-control" placeholder="Nhập email" />
                </div>
            </div>

            <div class="mb-3">
                <label class="form-label">Tên đăng nhập</label>
                <input asp-for="Profile.Username" class="form-control" placeholder="Nhập tên đăng nhập" />
            </div>

            <div class="d-flex justify-content-end">
               
                <button type="submit" class="btn px-4" style="background-color: #0d6efd; color: white;">Lưu thay đổi</button>

            </div> 
           

        </form>
    </div>
</div>
 *@
@page
@model PatientProfileModel
@{
    ViewData["Title"] = "Thông tin cá nhân";
}

<div class="d-flex justify-content-center align-items-center mt-5 mb-5">
    <div class="shadow-lg rounded overflow-hidden" style="min-width: 800px;">

        <!-- Header full width -->
        <div class="bg-primary bg-opacity-75 text-white text-center py-4">
            <h2 class="m-0">Thông tin cá nhân</h2>
        </div>

        <!-- Body form -->
        <div class="bg-light p-5">
            @if (Model.SuccessMessage != null)
            {
                <div class="alert alert-success text-center">@Model.SuccessMessage</div>
            }

            <form method="post">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">Họ</label>
                        <input asp-for="Profile.FirstName" class="form-control" />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Tên</label>
                        <input asp-for="Profile.LastName" class="form-control" />
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">Ngày sinh</label>
                        <input asp-for="Profile.Dob" type="date" class="form-control" />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Giới tính</label>
                        <select asp-for="Profile.Gender" class="form-select">
                            <option>Male</option>
                            <option>Female</option>
                            <option>Other</option>
                        </select>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">Số điện thoại</label>
                        <input asp-for="Profile.Phone" class="form-control" />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Địa chỉ</label>
                        <input asp-for="Profile.Address" class="form-control" />
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">Mã bảo hiểm</label>
                        <input asp-for="Profile.InsuranceNumber" class="form-control" />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Email</label>
                        <input asp-for="Profile.Email" class="form-control" />
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label">Tên đăng nhập</label>
                    <input asp-for="Profile.Username" class="form-control" />
                </div>

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary px-4">Lưu thay đổi</button>
                </div>
            </form>
        </div>
    </div>
</div>
